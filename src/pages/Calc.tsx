/** @format */

import React from 'react';
import { RouteComponentProps } from '@reach/router';

const Calc = (props: RouteComponentProps) => {
  const test = 'test';
  return <div>Calc</div>;
};

export default Calc;
