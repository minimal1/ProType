/** @format */

import * as React from 'react';
import { RouteComponentProps } from '@reach/router';
import Slide from '../Components/slide/Slide';

const PageSlide = (props: RouteComponentProps) => {
  return <Slide />;
};

export default PageSlide;
