/** @format */

import * as React from 'react';
import { RouteComponentProps } from '@reach/router';
import Calculator from '../Components/calc/Calculator';

const PageCalc = (props: RouteComponentProps) => {
  return <Calculator />;
};

export default PageCalc;
