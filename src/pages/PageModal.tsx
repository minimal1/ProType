/** @format */

import * as React from 'react';
import { RouteComponentProps } from '@reach/router';
import Modal from '../Components/modal/Modal';

const PageModal = (props: RouteComponentProps) => {
  return <Modal />;
};

export default PageModal;
``;
