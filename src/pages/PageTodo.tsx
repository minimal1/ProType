/** @format */

import * as React from 'react';
import { RouteComponentProps } from '@reach/router';
import Todolist from '../Components/todo/Todolist';

const PageTodo = (props: RouteComponentProps) => {
  return <Todolist />;
};

export default PageTodo;
